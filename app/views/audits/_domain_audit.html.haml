%li
  = link_to_domain_audit( domain_audit )
  %div[ domain_audit ]{ :style => 'display: none' }
    %strong= t :label_audit_time
    = domain_audit.created_at
    %br
    %strong= t :label_audit_changes
    %br
    = raw(display_hash domain_audit.audited_changes)
